# Seed memories (public)

Notes here are the twin's portable, versioned knowledge — reviewable in git
like the rest of `.twin/`. In v0 the soul (`../soul.md`) is the primary seed;
what the twin LEARNS at runtime lands in `.twin/private/` (gitignored,
on-device only) via its ManageMemory/ContextMemory agents.
